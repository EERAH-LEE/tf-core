#Terraform Core


테라폼으로 IaC를 배우는 수업의 예제들.

## 1. branch 생성/삭제 명령

```bash
$ git switch -c feature/<name>
$ git add work/<name>.tf
$ git commit -m 'feat: contaxt'
$ git push origin feature/<name>
```

## 2. PR 생성 (github 에서)
## 3. Review (github 에서)
## 4. Merge (github 에서)

## 5. main branch 동기화 & 작업 branch 정리

```
$ git checkout main
$ git
```